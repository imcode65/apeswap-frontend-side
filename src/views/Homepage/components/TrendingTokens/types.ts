export interface TokenInfo {
  tokenTicker: string
  tokenPrice: number
  percentChange: number
  contractAddress: string
  logoUrl: string
}

export interface TokenDisplayAmount {
  tokenStartIndex: number
  tokenEndIndex: number
}
